<!-- Default page template for all other pages -->
<div class="page-hero">
  <div class="container">
    <h1>{{ page.title }}</h1>
  </div>
</div>

<div class="container page-content">
  <div style="max-width: 800px; margin: 0 auto; font-size: 1.05rem; line-height: 1.8;">
    {{ page.content }}
  </div>
</div>
